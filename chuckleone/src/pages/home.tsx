import React from 'react';
import { Grid } from '@mui/material';

const home = () => {
    return (
        <Grid container spacing={2}>
            <Grid item sm={12}></Grid>
            <Grid item sm={12}></Grid>
        </Grid>
    )
};

export default home;